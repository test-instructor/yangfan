# Python 函数（pc）

Python 函数用于沉淀可复用的自定义逻辑，供接口/步骤/用例引用，例如签名计算、加解密、数据生成、复杂断言等。

## 适用场景

- 业务签名与鉴权：token 生成、header 计算
- 数据准备：随机数据、时间戳、结构化 payload 生成
- 结果处理：复杂断言、响应解析、变量二次加工

## 使用建议

- 尽量保持函数“纯函数化”（输入明确、输出明确），便于复用与定位
- 重要函数建议配合“调试信息（pcd）”进行验证与排障

