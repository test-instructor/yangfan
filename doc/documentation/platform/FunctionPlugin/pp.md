# Python 第三方库（pp）

用于管理运行环境中 Python 依赖（第三方库），以扩展函数插件与执行引擎的能力。

## 主要能力

- 安装/卸载/查看 Python 依赖（以平台实际能力为准）
- 让函数插件可以引用更多生态库（如 requests、cryptography 等）

## 使用建议

- 只安装必要依赖，避免依赖膨胀带来镜像体积与冲突风险
- 升级依赖前先在测试环境验证，确保历史用例不受影响

