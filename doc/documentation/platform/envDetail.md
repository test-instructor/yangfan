# 环境变量管理（envDetail）

环境变量用于管理多环境下可复用的参数集合，例如域名、账号、token、公共数据等。用例执行时可选择对应环境，自动注入变量，降低切换环境的成本。

## 主要能力

- 按环境（开发/测试/预发布/生产）维护变量集
- 支持变量在接口/步骤/用例中引用
- 支持变量导入导出与统一治理（以平台实际功能为准）

## 使用建议

- 将“域名、租户信息、测试账号、常用 header”纳入环境变量管理
- 敏感信息建议只在受控环境配置，避免写入用例正文

