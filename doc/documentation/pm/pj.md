# 项目配置（pj）

项目用于隔离测试资产与执行数据：接口、步骤、用例、任务、报告等都归属到某一个项目中，不同项目之间默认互相不可见。

## 适用场景

- 按产品线/业务域/团队拆分测试资产
- 同一套平台服务多套环境或多条流水线

## 主要能力

- 新建/编辑项目基础信息
- 维护项目级别的测试资产入口（接口、步骤、用例等）
- 作为权限控制与数据隔离的边界

## 推荐使用流程

1. 先创建项目
2. 再在该项目下维护环境变量、运行配置与函数插件
3. 最后在接口自动化模块中沉淀接口/步骤/用例，并通过定时任务执行

