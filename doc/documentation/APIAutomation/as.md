# 接口管理（as）

接口管理用于维护可复用的接口定义，是“步骤/用例/任务”复用链路的最底层资产。建议先把稳定的接口沉淀在这里，再通过步骤与用例进行场景编排。

## 主要能力

- 维护接口基本信息（方法、URL、请求头、参数、断言等）
- 支持从 CURL 导入接口
- 为步骤/用例提供可复用的接口模板

## 使用建议

- 按业务域或服务划分目录结构，便于复用与治理
- 将环境相关信息（域名、账号等）放入环境变量，避免硬编码

