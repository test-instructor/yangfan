# 测试用例（ac）

测试用例用于将“步骤”编排为完整场景（正向/异常/边界），并承载该场景的运行配置、数据驱动与断言策略，是回归执行的核心对象。

## 主要能力

- 编排步骤形成业务场景
- 配置用例级运行参数（优先级、标签、数据驱动等，以平台实际能力为准）
- 调试运行并生成执行报告

## 使用建议

- 用标签（如 smoke/regression/critical）管理用例集合，便于任务选择
- 用例里不要写死环境差异，统一从环境变量/运行配置引用

