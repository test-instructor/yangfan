# 测试步骤（acs）

测试步骤用于封装“可复用的业务动作”，把多个接口调用、变量提取与断言组合在一起，形成可编排的步骤资产。

## 典型内容

- 前置接口调用（登录、初始化数据）
- 变量提取与传递（从响应中导出 token/ID）
- 断言与校验（状态码、字段值、结构校验等）

## 使用建议

- 步骤尽量保持粒度适中：可复用、可组合、可维护
- 复杂逻辑用“Python 函数（pc）”抽离，步骤中只编排调用

